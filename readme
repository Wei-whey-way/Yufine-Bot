Bot program

.gitignore file contains a list of directores git would not put in
in config.json copy in all the userIDs that you want the slash command to work for

Note Guild means server

To run server (in directory folder): nodemon

To install slash commands: node src/register-commands.js

Look for the commands that are in yellow color with this () in the end. And can go to the disco
development portal to find out the parameters (e.g. isChatInputCommand())